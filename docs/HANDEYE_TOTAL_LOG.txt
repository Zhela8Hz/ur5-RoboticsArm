UR5 RGB hand-eye calibration total log
UR5 RGB 手眼标定总日志

Log update rule
---------------

中文：以后每次更新本日志时，必须同时写中文和 English 两个版本。中文版本中必须保留关键的不可替换英文原文名词，包括但不限于 `UR5`、`RGB`、`eye-in-hand`、`ChArUco`、`T_tool_camera`、`tool0`、`camera_color_optical_frame`、`base`、`PROJECT_ROOT`、`SCRIPT_DIR`、`handeye_result.yaml`、`samples.jsonl`、`translation_m_mean`、`rotation_deg_mean`、文件路径、命令、ROS topic、ROS frame、API/字段名和终端原始报错。

English: Every future update to this log must be written in both Chinese and English. The Chinese version must keep key non-translatable English terms verbatim, including but not limited to `UR5`, `RGB`, `eye-in-hand`, `ChArUco`, `T_tool_camera`, `tool0`, `camera_color_optical_frame`, `base`, `PROJECT_ROOT`, `SCRIPT_DIR`, `handeye_result.yaml`, `samples.jsonl`, `translation_m_mean`, `rotation_deg_mean`, file paths, commands, ROS topics, ROS frames, API/field names, and original terminal error text.


Current status
--------------

中文：

当前有效的 `RGB eye-in-hand` 外参结果是：

  calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

来源：

  robotic-arm-lab-vla/calibration/gemini335/handeye_tool0_camera_color_optical.yaml

说明：

  `calibration/extrinsics/handeye/sessions/20260630_195009` 是 DaBai / 旧安装状态历史结果，
  不再作为默认 `T_tool_camera` 使用。

当前离线状态：

  PASS_OFFLINE

在线验证状态：

  New bracket live validation passed in the source record. 在本机正式用于机器人操作前，应运行：

  calibration/extrinsics/handeye/scripts/start_handeye_live_validate.sh

English:

The current active `RGB eye-in-hand` extrinsic result is:

  calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

Source:

  robotic-arm-lab-vla/calibration/gemini335/handeye_tool0_camera_color_optical.yaml

Note:

  `calibration/extrinsics/handeye/sessions/20260630_195009` is a historical DaBai /
  old mounting result and is no longer the default `T_tool_camera`.

Current offline status:

  PASS_OFFLINE

Live validation status:

  New bracket live validation passed in the source record. Before using the result
  for robot operation on this computer, run:

  calibration/extrinsics/handeye/scripts/start_handeye_live_validate.sh


Current board and camera setup
------------------------------

中文：

当前默认使用的是大 `ChArUco` 板：

  squares: 6x6
  square_length_m: 0.040
  marker_length_m: 0.030
  dictionary: DICT_6X6_1000
  start_id: 233

当前 `RGB` 图像模式：

  1920x1080 @ 30 fps

旧 DaBai / 小板历史参数为：

  square_length_m: 0.025
  marker_length_m: 0.018

只有实际换回小板或复查旧 session 时，脚本参数才应切到 `0.025 / 0.018`。板子尺寸用错会导致 `rotation` 看起来正常，但 `translation` 出现厘米级误差。

English:

The default physical board is now the large `ChArUco` board:

  squares: 6x6
  square_length_m: 0.040
  marker_length_m: 0.030
  dictionary: DICT_6X6_1000
  start_id: 233

Current `RGB` image mode:

  1920x1080 @ 30 fps

The old DaBai / small-board historical parameters are:

  square_length_m: 0.025
  marker_length_m: 0.018

Use `0.025 / 0.018` only when switching back to the small board or reviewing old sessions. A wrong board size can make `rotation` look good while `translation` fails at centimeter scale.


Current active result
---------------------

中文：

Session：

  external source session 20260719_141204

单独结果报告：

  robotic-arm-lab-vla/calibration/gemini335/handeye_session_20260719_141204_summary.md

Result YAML：

  calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

Validation CSV：

  Not copied into this repository. Source summary reports online RTDE live validation.

标定类型：

  eye-in-hand

Method：

  Daniilidis

Transform：

  T_tool_camera
  parent_frame = tool0
  child_frame  = camera_color_optical_frame

`tool0` frame 下的 translation：

  x = -0.1036245277 m = -103.62 mm
  y = -0.0101101606 m =  -10.11 mm
  z =  0.0865760371 m =   86.58 mm

Homogeneous matrix `T_tool_camera`：

   0.008140661  -0.853384425   0.521218527  -0.103624528
   0.999769910   0.017290676   0.012694879  -0.010110161
  -0.019845832   0.520995255   0.853328827   0.086576037
   0.000000000   0.000000000   0.000000000   1.000000000

Offline fixed-board consistency：

  translation_m_mean = 0.001051101 m = 1.05 mm
  translation_m_max  = 0.001668065 m = 1.67 mm
  rotation_deg_mean  = 0.118467 deg
  rotation_deg_max   = 0.261194 deg

Online RTDE live validation scatter：

  validation samples = 6
  translation mean   = 0.83 mm
  translation max    = 1.53 mm
  rotation mean      = 0.24 deg
  rotation max       = 0.43 deg

English:

Session:

  external source session 20260719_141204

Standalone result report:

  robotic-arm-lab-vla/calibration/gemini335/handeye_session_20260719_141204_summary.md

Result YAML:

  calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

Validation CSV:

  Not copied into this repository. Source summary reports online RTDE live validation.

Calibration type:

  eye-in-hand

Method:

  Daniilidis

Transform:

  T_tool_camera
  parent_frame = tool0
  child_frame  = camera_color_optical_frame

Translation in the `tool0` frame:

  x = -0.1036245277 m = -103.62 mm
  y = -0.0101101606 m =  -10.11 mm
  z =  0.0865760371 m =   86.58 mm

Homogeneous matrix `T_tool_camera`:

   0.008140661  -0.853384425   0.521218527  -0.103624528
   0.999769910   0.017290676   0.012694879  -0.010110161
  -0.019845832   0.520995255   0.853328827   0.086576037
   0.000000000   0.000000000   0.000000000   1.000000000

Offline fixed-board consistency:

  translation_m_mean = 0.001051101 m = 1.05 mm
  translation_m_max  = 0.001668065 m = 1.67 mm
  rotation_deg_mean  = 0.118467 deg
  rotation_deg_max   = 0.261194 deg

Online RTDE live validation scatter:

  validation samples = 6
  translation mean   = 0.83 mm
  translation max    = 1.53 mm
  rotation mean      = 0.24 deg
  rotation max       = 0.43 deg


2026-07-22 default hand-eye program switch
------------------------------------------

中文：

1. 将当前项目默认 `RGB eye-in-hand` 结果切换为 Gemini335 新转接件结果：

   calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

2. 该文件来自：

   robotic-arm-lab-vla/calibration/gemini335/handeye_tool0_camera_color_optical.yaml

3. 默认 `ChArUco` 参数切换为大板：

   squares: 6x6
   square_length_m: 0.040
   marker_length_m: 0.030
   dictionary: DICT_6X6_1000
   start_id: 233

4. 默认求解方法切换为 `Daniilidis`。旧 DaBai / 小板 `Tsai` 结果和历史 session 保留，但不再作为默认程序入口使用。

5. 本次没有连接真实设备，没有执行机器人运动，没有使用 `sudo`，没有执行 Git 写操作。

English:

1. Switched the current project's default `RGB eye-in-hand` result to the Gemini335 new-bracket result:

   calibration/extrinsics/handeye/handeye_tool0_camera_color_optical.yaml

2. The file was sourced from:

   robotic-arm-lab-vla/calibration/gemini335/handeye_tool0_camera_color_optical.yaml

3. Default `ChArUco` parameters now use the large board:

   squares: 6x6
   square_length_m: 0.040
   marker_length_m: 0.030
   dictionary: DICT_6X6_1000
   start_id: 233

4. The default solve method is now `Daniilidis`. The old DaBai / small-board `Tsai` result and historical sessions are preserved, but they are no longer used as default program entries.

5. This update did not connect real hardware, execute robot motion, use `sudo`, or run Git write operations.


2026-07-22 Ceres drift correction prototype
-------------------------------------------

中文：

1. 新增 C++ / Ceres 漂移修正原型：

   calibration/extrinsics/handeye/tools/optimize_handeye_drift_ceres.cpp
   calibration/extrinsics/handeye/tools/CMakeLists.txt

2. `handeye_live_validate.py` 已扩展记录：

   T_base_tool
   T_camera_target
   drift_xyz_m
   drift_rpy_deg

   这些字段用于离线 Ceres 优化，不改变真实 `handeye` YAML。

3. `start_handeye_live_validate.sh` 支持环境变量：

   HANDEYE_DRIFT_X_M
   HANDEYE_DRIFT_Y_M
   HANDEYE_DRIFT_Z_M
   HANDEYE_DRIFT_ROLL_DEG
   HANDEYE_DRIFT_PITCH_DEG
   HANDEYE_DRIFT_YAW_DEG
   HANDEYE_LIVE_OUTPUT

4. Ceres 优化目标：

   对每个样本计算

     T_base_board_i =
       T_base_tool_i * T_tool_camera * T_applied_drift * T_correction * T_camera_target_i

   让所有 `T_base_board_i` 尽量一致，同时估计 `T_correction`。

5. 本机检查结果：

   `cmake`、`g++`、Eigen 可用；未发现 Ceres 开发包，`cmake` 报错缺少
   `CeresConfig.cmake`。没有执行 `sudo` 安装。

English:

1. Added a C++ / Ceres drift-correction prototype:

   calibration/extrinsics/handeye/tools/optimize_handeye_drift_ceres.cpp
   calibration/extrinsics/handeye/tools/CMakeLists.txt

2. `handeye_live_validate.py` now records:

   T_base_tool
   T_camera_target
   drift_xyz_m
   drift_rpy_deg

   These fields support offline Ceres optimization and do not modify the real
   `handeye` YAML.

3. `start_handeye_live_validate.sh` supports these environment variables:

   HANDEYE_DRIFT_X_M
   HANDEYE_DRIFT_Y_M
   HANDEYE_DRIFT_Z_M
   HANDEYE_DRIFT_ROLL_DEG
   HANDEYE_DRIFT_PITCH_DEG
   HANDEYE_DRIFT_YAW_DEG
   HANDEYE_LIVE_OUTPUT

4. Ceres objective:

   For each sample, compute

     T_base_board_i =
       T_base_tool_i * T_tool_camera * T_applied_drift * T_correction * T_camera_target_i

   and make all `T_base_board_i` as consistent as possible while estimating
   `T_correction`.

5. Local check result:

   `cmake`, `g++`, and Eigen are available. The Ceres development package was
   not found, and `cmake` failed because `CeresConfig.cmake` is missing. No
   `sudo` installation was executed.


2026-06-30 work progress
------------------------

中文：

1. 复查 `2026-06-26` 旧 `RGB hand-eye calibration` 日志。结论：旧结果只适用于旧相机安装状态；由于 `RGB camera/end-effector mount` 后来拆装重装，旧 `T_tool_camera` 对当前硬件失效。

2. 确认当前脚本使用动态路径：通过 `git rev-parse --show-toplevel` 解析 `PROJECT_ROOT`，失败时从 `SCRIPT_DIR` 做相对路径回退。

3. 今天第一次采集 session 为：

   calibration/extrinsics/handeye/sessions/20260630_190249

   最初用大板参数 `square_length_m=0.040`、`marker_length_m=0.030` 求解，表现为 `rotation` 一致性好但 `translation` 有厘米级误差。

4. 确认 `RGB` 分辨率保持 `640x360`。临时 `640x480` 改动已撤回，当前相机启动参数为：

   color_width:=640 color_height:=360 color_fps:=10

5. 今天第二次采集 session 为：

   calibration/extrinsics/handeye/sessions/20260630_195009

   它最初也被大板参数误算，导致厘米级 `translation` error。

6. 确认实际物理板为小 `ChArUco` 板：`25 mm squares`、`18 mm markers`。随后将 active `hand-eye` 和 `RGB ChArUco` 工具参数切回：

   square_length_m=0.025
   marker_length_m=0.018

7. 使用正确小板参数离线重算第二次采集，结果为 `PASS_OFFLINE`。当前 active result 为：

   calibration/extrinsics/handeye/sessions/20260630_195009/handeye_result.yaml

8. 使用同一小板参数离线重算第一次采集，用于对比：

   calibration/extrinsics/handeye/sessions/20260630_190249/handeye_result_small_board.yaml

9. 对比后决定使用第二次采集 `20260630_195009`，因为它的 `translation` 一致性和 fixed-board scatter 更小。

English:

1. Reviewed the old `2026-06-26` `RGB hand-eye calibration` log. Conclusion: the old result was valid only for the previous camera mounting. Since the `RGB camera/end-effector mount` was later disassembled and reinstalled, the old `T_tool_camera` is invalid for the current hardware.

2. Verified that current scripts use dynamic paths: `PROJECT_ROOT` is resolved via `git rev-parse --show-toplevel`, with a fallback relative to `SCRIPT_DIR`.

3. The first capture session today was:

   calibration/extrinsics/handeye/sessions/20260630_190249

   It was initially solved with the large-board parameters `square_length_m=0.040` and `marker_length_m=0.030`, producing good `rotation` consistency but centimeter-scale `translation` error.

4. Confirmed that `RGB` resolution remains `640x360`. Temporary `640x480` changes were reverted. Current camera launch parameters are:

   color_width:=640 color_height:=360 color_fps:=10

5. The second capture session today was:

   calibration/extrinsics/handeye/sessions/20260630_195009

   It was also initially solved with the large-board parameters, causing centimeter-scale `translation` error.

6. Confirmed that the physical board is the small `ChArUco` board: `25 mm squares` and `18 mm markers`. The active `hand-eye` and `RGB ChArUco` tooling was switched back to:

   square_length_m=0.025
   marker_length_m=0.018

7. Recomputed the second capture offline with the correct small-board parameters. The result is `PASS_OFFLINE`. Current active result:

   calibration/extrinsics/handeye/sessions/20260630_195009/handeye_result.yaml

8. Recomputed the first capture offline with the same small-board parameters for comparison:

   calibration/extrinsics/handeye/sessions/20260630_190249/handeye_result_small_board.yaml

9. Decision: use the second capture `20260630_195009`, because its `translation` consistency and fixed-board scatter are smaller.


Comparison of 2026-06-30 captures
---------------------------------

中文：

第一次采集 `20260630_190249`，用小板参数重算：

  samples = 27
  translation_m_mean = 0.003728 m = 3.73 mm
  translation_m_max  = 0.005513 m = 5.51 mm
  rotation_deg_mean  = 0.238463 deg
  rotation_deg_max   = 0.476281 deg
  scatter_mean       = 1.37 mm
  scatter_max        = 3.53 mm

第二次采集 `20260630_195009`，用小板参数重算：

  samples = 25
  translation_m_mean = 0.001362 m = 1.36 mm
  translation_m_max  = 0.003045 m = 3.04 mm
  rotation_deg_mean  = 0.288144 deg
  rotation_deg_max   = 0.689497 deg
  scatter_mean       = 1.04 mm
  scatter_max        = 3.04 mm

决策：使用第二次采集 `20260630_195009`。

English:

First capture `20260630_190249`, recomputed with small-board parameters:

  samples = 27
  translation_m_mean = 0.003728 m = 3.73 mm
  translation_m_max  = 0.005513 m = 5.51 mm
  rotation_deg_mean  = 0.238463 deg
  rotation_deg_max   = 0.476281 deg
  scatter_mean       = 1.37 mm
  scatter_max        = 3.53 mm

Second capture `20260630_195009`, recomputed with small-board parameters:

  samples = 25
  translation_m_mean = 0.001362 m = 1.36 mm
  translation_m_max  = 0.003045 m = 3.04 mm
  rotation_deg_mean  = 0.288144 deg
  rotation_deg_max   = 0.689497 deg
  scatter_mean       = 1.04 mm
  scatter_max        = 3.04 mm

Decision: use the second capture `20260630_195009`.


Script and input file policy
----------------------------

中文：

脚本通过以下模式动态解析仓库根目录：

  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  PROJECT_ROOT="$(git -C "${SCRIPT_DIR}" rev-parse --show-toplevel 2>/dev/null || true)"

如果无法解析 git root，脚本会从自身位置按相对路径回退。

关键脚本：

  scripts/camera/start_dabai_camera.sh
  scripts/robot/start_ur5_driver.sh
  scripts/robot/check_ur5_state.sh
  calibration/extrinsics/handeye/tools/handeye_check_env.sh
  calibration/extrinsics/handeye/scripts/start_handeye_retake_capture.sh
  calibration/extrinsics/handeye/scripts/solve_latest_handeye_retake.sh
  calibration/extrinsics/handeye/scripts/start_handeye_live_validate.sh

关键输入文件：

  configs/robot/ur5_actual_calibration.yaml
  calibration/rgb_intrinsics/results/rgb_intrinsics_640x360.yaml

English:

Scripts dynamically resolve the repository root using:

  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  PROJECT_ROOT="$(git -C "${SCRIPT_DIR}" rev-parse --show-toplevel 2>/dev/null || true)"

If the git root cannot be resolved, scripts fall back to a relative path from their own location.

Important scripts:

  scripts/camera/start_dabai_camera.sh
  scripts/robot/start_ur5_driver.sh
  scripts/robot/check_ur5_state.sh
  calibration/extrinsics/handeye/tools/handeye_check_env.sh
  calibration/extrinsics/handeye/scripts/start_handeye_retake_capture.sh
  calibration/extrinsics/handeye/scripts/solve_latest_handeye_retake.sh
  calibration/extrinsics/handeye/scripts/start_handeye_live_validate.sh

Important input files:

  configs/robot/ur5_actual_calibration.yaml
  calibration/rgb_intrinsics/results/rgb_intrinsics_640x360.yaml


Archived invalid 2026-06-26 result
----------------------------------

中文：

`2026-06-26` 结果在 `RGB camera/end-effector` 拆装重装后失效。它只作为历史数值参考保留，不能用于当前操作。

归档结果：

  calibration/extrinsics/handeye/sessions/20260626_004119/handeye_result.yaml

旧 `T_tool_camera` translation：

  x =  0.0652314244 m =  65.23 mm
  y = -0.0071898034 m =  -7.19 mm
  z =  0.0094051179 m =   9.41 mm

旧 homogeneous matrix `T_tool_camera`：

  -0.003008205  -0.999669798   0.025519506   0.065231424
   0.999991855  -0.002938546   0.002766705  -0.007189803
  -0.002690801   0.025527621   0.999670496   0.009405118
   0.000000000   0.000000000   0.000000000   1.000000000

English:

The `2026-06-26` result became invalid after the `RGB camera/end-effector` was disassembled and reinstalled. It is kept only as a historical numeric reference and must not be used for current operation.

Archived result:

  calibration/extrinsics/handeye/sessions/20260626_004119/handeye_result.yaml

Old `T_tool_camera` translation:

  x =  0.0652314244 m =  65.23 mm
  y = -0.0071898034 m =  -7.19 mm
  z =  0.0094051179 m =   9.41 mm

Old homogeneous matrix `T_tool_camera`:

  -0.003008205  -0.999669798   0.025519506   0.065231424
   0.999991855  -0.002938546   0.002766705  -0.007189803
  -0.002690801   0.025527621   0.999670496   0.009405118
   0.000000000   0.000000000   0.000000000   1.000000000


Preserved troubleshooting notes
-------------------------------

中文：

`UR driver` 必须使用真实机器人运动学文件：

  configs/robot/ur5_actual_calibration.yaml

曾经出现的运动学不匹配报错原文：

  The calibration parameters of the connected robot don't match the ones from the given kinematics config file.

含义：`ROS driver` 使用的运动学与真实 `UR5` 不一致，会导致 `base -> tool0` 不准确，并放大 `hand-eye` 验证误差。

期望成功信息原文包括：

  Calibration checksum: calib_17294063568689657538
  Calibration checked successfully.
  System successfully started.
  Robot mode: RUNNING
  Safety mode: NORMAL

曾经出现的非致命消息原文：

  A message was lost!!!

含义：短暂运行 `ros2 topic echo` 时可能出现；如果 `/joint_states` 和 `base -> tool0` TF 正常，则不是失败。

曾经出现的真实失败原文：

  WARNING: topic [/joint_states] does not appear to be published yet
  Could not determine the type for the passed topic
  ERROR: /joint_states has no data yet.

含义：`UR driver` core node 未运行或已退出，导致 `/joint_states` 未发布。

曾经出现的 `UR driver` fatal error 原文：

  Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)

含义：driver 部分连接到机器人，但没有及时收到 robot configuration package。重启 `UR driver`，并确认 teach pendant 上的 `External Control` 程序处于 Play 状态。

English:

The `UR driver` must use the real robot kinematics file:

  configs/robot/ur5_actual_calibration.yaml

Previously observed kinematics mismatch text:

  The calibration parameters of the connected robot don't match the ones from the given kinematics config file.

Meaning: the `ROS driver` was using kinematics that did not match the physical `UR5`, which can make `base -> tool0` inaccurate and increase `hand-eye` validation error.

Expected successful messages include:

  Calibration checksum: calib_17294063568689657538
  Calibration checked successfully.
  System successfully started.
  Robot mode: RUNNING
  Safety mode: NORMAL

Previously observed non-fatal message:

  A message was lost!!!

Meaning: this can appear when `ros2 topic echo` subscribes briefly. It is not a failure if `/joint_states` and `base -> tool0` TF are valid.

Previously observed real failure:

  WARNING: topic [/joint_states] does not appear to be published yet
  Could not determine the type for the passed topic
  ERROR: /joint_states has no data yet.

Meaning: the `UR driver` core node was not running or had exited, so `/joint_states` was not being published.

Previously observed `UR driver` fatal error:

  Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)

Meaning: the driver partially connected to the robot but did not receive the robot configuration package quickly enough. Restart `UR driver` and confirm that the `External Control` program is playing on the teach pendant.

================================================================================
2026-07-05 Gemini335 RGB Intrinsics Update
================================================================================

Context:

  Camera: Orbbec Gemini 335
  USB: USB3.2 confirmed by `orbbec_camera_node`
  ROS: Humble
  ROS wrapper: ros-humble-orbbec-camera 2.8.6
  Orbbec SDK: 2.8.6
  Firmware: 1.4.60
  Resolution used: 1920x1080
  FPS used: 30
  Format used: MJPG

Important issue found:

  A stale `orbbec_camera_node` process was still holding the USB device.
  This caused:

    uvc_open failed: [Path: 2-2-7.0, Return Code: -6]

  After stopping that stale process, `list_camera_profile_mode_node` and the
  RGB camera node could open Gemini335 normally.

ChArUco board actually used:

  Board type: small board
  squares_x: 6
  squares_y: 6
  square_length_m: 0.025
  marker_length_m: 0.018
  dictionary: DICT_6X6_1000
  start_id: 233
  min_charuco_corners: 20

Note:

  The 2026-07-05 RGB intrinsics run used the small-board parameters above.
  It did not use the historical large-board values:

    square_length_m=0.040
    marker_length_m=0.030

Capture method:

  The RGB intrinsics node was updated to behave like the previous trigger
  workflow: each `/capture` service call processes the latest image, accepts
  the frame only if enough ChArUco corners are detected, and saves raw/overlay
  images to:

    /tmp/gemini335_rgb_intrinsics_captures

  20 accepted observations were collected. Some rejected attempts had too few
  ChArUco corners because the board was partly out of view or too steeply
  angled.

Generated candidate intrinsics:

  calibration/rgb_intrinsics/results/rgb_intrinsics_gemini335_1920x1080.yaml

Calibration result:

  image_width: 1920
  image_height: 1080
  camera_name: gemini335_color
  fx: 1035.5285795747313
  fy: 1037.4036932657955
  cx: 973.143653231648
  cy: 533.5388236488085
  distortion:
    k1: 0.010060127630146826
    k2: -0.08440653144268961
    p1: -0.0031529422933081357
    p2: 0.003274915369901648
    k3: 0.10116048697079912
  calibration_reprojection_error_px: 0.3788229893599487

SDK factory intrinsics observed from `/camera/color/camera_info`:

  fx: 1039.3955078125
  fy: 1040.1788330078125
  cx: 964.787109375
  cy: 542.3262939453125
  distortion:
    k1: 0.008620047010481358
    k2: -0.05229831486940384
    p1: -0.00038759250310249627
    p2: -0.00007887257379479706
    k3: 0.035471539944410324

Validation comparison:

  Previous SDK validation before recalibration:

    valid_samples: 10
    rmse_px_mean: 0.525
    rmse_px_median: 0.572
    rmse_px_max: 0.900
    mean_px_mean: 0.476
    max_px_max: 1.813

  SDK validation after the 20-frame capture session, using current board poses:

    valid_samples: 10
    rmse_px_mean: 1.060
    rmse_px_median: 0.754
    rmse_px_max: 2.631
    mean_px_mean: 0.939
    max_px_max: 5.147

  New YAML validation after the 20-frame capture session, using current board poses:

    valid_samples: 10
    rmse_px_mean: 0.958
    rmse_px_median: 0.727
    rmse_px_max: 2.025
    mean_px_mean: 0.848
    max_px_max: 4.082

Interpretation:

  The new Gemini335 intrinsics are slightly better than the SDK factory
  intrinsics in the later validation run, but the improvement is modest.
  The calibration solve reprojection error of about 0.379 px is usable as a
  candidate, but it is not as clean as the earlier DaBai calibration around
  0.2 px.

  For precise hand-eye calibration, this Gemini335 intrinsics result should be
  treated as acceptable but not yet ideal. A better dedicated intrinsics session
  should collect more diverse and cleaner board poses: full board visible,
  sharp focus, good lighting, broad image coverage including corners, multiple
  distances, and fewer near-edge partial detections.

================================================================================
2026-07-07 UR5 / Gemini335 Hand-eye Software Path Probe
================================================================================

中文：

目标：

  在相机固定件尚未打印完成前，验证 `hand-eye` 软件路径是否能跑通。
  本次目标不是生成可长期使用的正式 `T_tool_camera`，而是确认：

    - UR5 网络连通；
    - `ur_robot_driver` 当前问题边界；
    - `ur_rtde` 是否能读取机器人位姿；
    - Gemini335 RGB 相机是否能发布图像和 `CameraInfo`；
    - ChArUco 检测是否能得到 `T_camera_target`；
    - 能否生成兼容现有 solver 的 `samples.jsonl` 记录。

UR5 网络和 Dashboard 状态：

  `ping 192.168.1.10` 成功，0% packet loss。

  Dashboard 只读查询确认：

    Robotmode: RUNNING
    Safetymode: NORMAL
    Program running: false
    No program loaded
    STOPPED null
    URSoftware 3.15.8.106339

  本机有线网卡和路由：

    enp7s0: 192.168.1.20/24
    192.168.1.10 dev enp7s0 src 192.168.1.20

`ur_robot_driver` 观察结果：

  多次运行 `scripts/robot/start_ur5_driver.sh`，Dashboard 和 RTDE 初步连接成功：

    Connected: Universal Robots Dashboard Server
    Successfully connected to Dashboard Server at 192.168.1.10. Robot has version 3.15.8.0
    Negotiated RTDE protocol version to 2.
    Received URControl version 3.15.8.0
    Setting up RTDE communication with frequency 125.000000

  但 `ur_ros2_control_node` 多次卡在 configuration package：

    Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)
    Failed to set the initial state of the component : ur5 to active

  该问题表现为间歇性：同一配置下曾有一次成功拿到：

    Calibration checksum: 'calib_17294063568689657538'.
    Calibration checked successfully.
    System successfully started!

  但后续仍可复现 timeout。为降低人工重复操作，`scripts/robot/start_ur5_driver.sh`
  已增加：

    - 启动前检查 UR primary interface `192.168.1.10:30001` 是否可读；
    - 检测到 `Could not get configuration package within timeout` 后自动重试；
    - 每次 launch 使用独立 process group，失败时整组清理，避免残留 controller 冲突；
    - 默认最大尝试次数 `UR5_DRIVER_MAX_ATTEMPTS=3`。

  注意：该修改只改善启动重试流程，不会发送机器人运动指令。

External Control / IP 说明：

  `robot_ip:=192.168.1.10` 是 UR5 控制柜 IP，方向是电脑连接机器人。

  `reverse_ip:=192.168.1.20` 和示教器 `External Control Host IP`
  应该是电脑有线网卡 IP，方向是机器人回连电脑。

  用户确认当前 TCP 名称为 `tcp_`，TCP 的 `x y z rx ry rz` 全部为 0。
  因此本次 `ur_rtde.getActualTCPPose()` 可按 `base -> tool0` 使用。
  `cz=60.0 mm` 是负载重心参数，不改变 TCP 几何坐标系。

`ur_rtde` 验证：

  本机安装了 Python 包：

    ur_rtde 1.6.3

  使用 `rtde_receive.RTDEReceiveInterface("192.168.1.10")` 可直接读取：

    getActualTCPPose()
    getActualQ()

  一次读数示例：

    connected True
    TCP Pose: -0.349319 -0.502460 0.389995 0.979930 1.526973 1.552238
    Joints: 0.852055 -1.580669 1.579880 -1.559735 -1.576649 0.389503

  结论：即使 `ur_robot_driver` 当前不稳定，`ur_rtde` 仍可提供手眼采集所需的
  机器人位姿输入。因为当前 TCP offset 为 0，本次可将 RTDE TCP pose 作为
  `T_base_tool` 写入样本。后续若 TCP offset 被修改，必须重新确认坐标定义。

Gemini335 相机验证：

  相机设备：

    Orbbec Gemini 335
    Serial number: CP0F46300089
    Firmware version: 1.4.60
    ROS Wrapper version: 2.8.6
    SDK version: 2.8.6
    usb connect type: USB3.2

  普通沙箱内启动 Orbbec 节点会失败：

    Usb pal is not exist, please check the build config that you have enabled BUILD_USB_PAL

  原因：普通沙箱看不到 `/dev/bus/usb`，Orbbec SDK 无法访问 USB PAL。
  在沙箱外运行 `ros2 launch orbbec_camera gemini_330_series.launch.py ...`
  后，相机节点正常启动。

  已确认 ROS topic：

    /camera/color/image_raw
    /camera/color/camera_info

  `CameraInfo`：

    frame_id: camera_color_optical_frame
    width: 1920
    height: 1080
    distortion_model: plumb_bob

  图像流实测约 14 Hz。本次启动请求为 `1920x1080 @ 30 fps MJPG`，
  实测频率低于请求值，但足以做手眼路径验证。

ChArUco 检测验证：

  小板参数：

    squares_x: 6
    squares_y: 6
    square_length_m: 0.025
    marker_length_m: 0.018
    dictionary: DICT_6X6_1000
    start_id: 233
    min_charuco_corners: 20

  单帧检测成功：

    markers: 18
    charuco: 25

  说明图像进入 OpenCV / ChArUco 检测链路正常，可得到 `T_camera_target`。

RTDE hand-eye probe 工具：

  新增工具：

    calibration/extrinsics/handeye/tools/handeye_capture_rtde.py

  作用：

    - 订阅 `/camera/color/image_raw`；
    - 使用 Gemini335 1920x1080 内参；
    - 检测 ChArUco 并估计 `T_camera_target`；
    - 使用 `ur_rtde` 读取 `getActualTCPPose()` 和 `getActualQ()`；
    - 将 RTDE TCP pose 转成 `T_base_tool`；
    - 写出与现有 `handeye_solve.py` 兼容的 `samples.jsonl`。

  本次曾创建临时 session：

    calibration/extrinsics/handeye/sessions/20260707_204658_rtde_probe

  其中 `sample_001` 曾采集成功：

    charuco_corners: 25
    robot_pose_source: ur_rtde

  按用户要求，该单帧样本、raw image、overlay image、`samples.jsonl`、
  临时 session 目录和 `latest_rtde_probe` symlink 已删除。

当前结论：

  已跑通：

    Gemini335 RGB image
    -> ChArUco `T_camera_target`
    -> `ur_rtde` robot pose
    -> `T_base_tool`
    -> solver-compatible `samples.jsonl` format

  未完成：

    未采满 8 个以上样本，因此未运行 `handeye_solve.py` 求解正式或临时
    `T_tool_camera`。现有 solver 最少需要 8 个样本，建议 12-15 个不同姿态。

  本次没有执行机器人运动，没有使用 sudo，没有执行 Git 写操作。

English:

Goal:

  Before the camera mount is printed and installed, the purpose was to verify
  the `hand-eye` software path. This run was not intended to produce a durable
  production `T_tool_camera`; it was intended to confirm:

    - UR5 network connectivity;
    - the current boundary of the `ur_robot_driver` issue;
    - whether `ur_rtde` can read robot poses;
    - whether Gemini335 RGB publishes images and `CameraInfo`;
    - whether ChArUco detection can produce `T_camera_target`;
    - whether a solver-compatible `samples.jsonl` record can be produced.

UR5 network and Dashboard state:

  `ping 192.168.1.10` succeeded with 0% packet loss.

  Dashboard read-only queries showed:

    Robotmode: RUNNING
    Safetymode: NORMAL
    Program running: false
    No program loaded
    STOPPED null
    URSoftware 3.15.8.106339

  Local wired interface and route:

    enp7s0: 192.168.1.20/24
    192.168.1.10 dev enp7s0 src 192.168.1.20

`ur_robot_driver` observations:

  Several runs of `scripts/robot/start_ur5_driver.sh` reached Dashboard and RTDE:

    Connected: Universal Robots Dashboard Server
    Successfully connected to Dashboard Server at 192.168.1.10. Robot has version 3.15.8.0
    Negotiated RTDE protocol version to 2.
    Received URControl version 3.15.8.0
    Setting up RTDE communication with frequency 125.000000

  However, `ur_ros2_control_node` repeatedly failed while waiting for the
  configuration package:

    Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)
    Failed to set the initial state of the component : ur5 to active

  The issue is intermittent. With the same configuration, one run did reach:

    Calibration checksum: 'calib_17294063568689657538'.
    Calibration checked successfully.
    System successfully started!

  But later runs still reproduced the timeout. To reduce manual retry work,
  `scripts/robot/start_ur5_driver.sh` was updated to:

    - check whether UR primary interface `192.168.1.10:30001` is readable before launch;
    - retry automatically when `Could not get configuration package within timeout` is detected;
    - launch each attempt in a separate process group and clean up the whole group on failure;
    - use `UR5_DRIVER_MAX_ATTEMPTS=3` by default.

  This only improves startup retry behavior. It does not send robot motion
  commands.

External Control / IP clarification:

  `robot_ip:=192.168.1.10` is the UR5 controller IP, used by the computer to
  connect to the robot.

  `reverse_ip:=192.168.1.20` and the teach pendant `External Control Host IP`
  should be the wired IP of this computer, used by the robot to connect back to
  the driver.

  The user confirmed the active TCP is named `tcp_` and its `x y z rx ry rz`
  values are all zero. Therefore `ur_rtde.getActualTCPPose()` can be treated as
  `base -> tool0` for this probe. `cz=60.0 mm` is the payload center of gravity
  parameter and does not change the geometric TCP frame.

`ur_rtde` verification:

  The Python package was installed:

    ur_rtde 1.6.3

  `rtde_receive.RTDEReceiveInterface("192.168.1.10")` can read:

    getActualTCPPose()
    getActualQ()

  Example output:

    connected True
    TCP Pose: -0.349319 -0.502460 0.389995 0.979930 1.526973 1.552238
    Joints: 0.852055 -1.580669 1.579880 -1.559735 -1.576649 0.389503

  Conclusion: even while `ur_robot_driver` is unstable, `ur_rtde` can provide
  the robot pose input needed for hand-eye capture. Since the current TCP offset
  is zero, the RTDE TCP pose can be written as `T_base_tool` for this probe.
  If the TCP offset changes later, the frame definition must be checked again.

Gemini335 camera verification:

  Camera:

    Orbbec Gemini 335
    Serial number: CP0F46300089
    Firmware version: 1.4.60
    ROS Wrapper version: 2.8.6
    SDK version: 2.8.6
    usb connect type: USB3.2

  Starting the Orbbec node inside the normal sandbox failed with:

    Usb pal is not exist, please check the build config that you have enabled BUILD_USB_PAL

  Cause: the normal sandbox cannot see `/dev/bus/usb`, so Orbbec SDK cannot
  access the USB PAL. Running `ros2 launch orbbec_camera gemini_330_series.launch.py ...`
  outside the sandbox started the camera node successfully.

  Confirmed ROS topics:

    /camera/color/image_raw
    /camera/color/camera_info

  `CameraInfo`:

    frame_id: camera_color_optical_frame
    width: 1920
    height: 1080
    distortion_model: plumb_bob

  The image stream measured around 14 Hz. The requested mode was
  `1920x1080 @ 30 fps MJPG`; the measured rate is lower than requested, but
  sufficient for software path validation.

ChArUco detection verification:

  Small board parameters:

    squares_x: 6
    squares_y: 6
    square_length_m: 0.025
    marker_length_m: 0.018
    dictionary: DICT_6X6_1000
    start_id: 233
    min_charuco_corners: 20

  One frame was detected successfully:

    markers: 18
    charuco: 25

  This confirms that the image can enter the OpenCV / ChArUco detection path
  and produce `T_camera_target`.

RTDE hand-eye probe tool:

  New tool:

    calibration/extrinsics/handeye/tools/handeye_capture_rtde.py

  Purpose:

    - subscribe to `/camera/color/image_raw`;
    - use Gemini335 1920x1080 intrinsics;
    - detect ChArUco and estimate `T_camera_target`;
    - use `ur_rtde` to read `getActualTCPPose()` and `getActualQ()`;
    - convert RTDE TCP pose to `T_base_tool`;
    - write `samples.jsonl` compatible with existing `handeye_solve.py`.

  A temporary session was created:

    calibration/extrinsics/handeye/sessions/20260707_204658_rtde_probe

  `sample_001` was successfully captured:

    charuco_corners: 25
    robot_pose_source: ur_rtde

  Per user request, that single sample, raw image, overlay image, `samples.jsonl`,
  temporary session directory, and `latest_rtde_probe` symlink were deleted.

Current conclusion:

  Verified path:

    Gemini335 RGB image
    -> ChArUco `T_camera_target`
    -> `ur_rtde` robot pose
    -> `T_base_tool`
    -> solver-compatible `samples.jsonl` format

  Not completed:

    Fewer than 8 samples were collected, so `handeye_solve.py` was not run to
    solve a formal or temporary `T_tool_camera`. The current solver requires at
    least 8 samples; 12-15 diverse poses are recommended.

  No robot motion command was executed. No sudo was used. No Git write
  operation was performed.

================================================================================
2026-07-07 GitHub backup and documentation update
================================================================================

中文：

目标：

  将今天 Gemini335 相关代码、脚本、配置、日志和说明文档备份到 GitHub，
  并与 DaBai 相机时期的内容区分。

GitHub 远端核对：

  远端仓库：

    https://github.com/Zhela8Hz/ur5-RoboticsArm

  通过只读命令核对远端引用：

    git ls-remote --symref origin HEAD
    git ls-remote origin

  结果显示，GitHub 远端默认 HEAD 当前指向：

    refs/heads/calibration_gemini335
    commit 986ab9f060d769d6e9016218871e410f80d21b34

  远端当前可见分支包括：

    calibration_gemini335 -> 986ab9f060d769d6e9016218871e410f80d21b34
    calibration_dabai     -> 1f1a32101caba9c346c4da17b923e5ef1d27e63f
    old-main-backup       -> 1f1a32101caba9c346c4da17b923e5ef1d27e63f
    organize-calibration-layout -> 6bbc92ecb217d917375629431eead073ca942324

  结论：

    GitHub 远端默认打开的分支已经是 `calibration_gemini335`，对应今天
    Gemini335 的最新备份提交 `986ab9f`。远端还保留了 DaBai / 旧 main
    内容对应的 `calibration_dabai` 和 `old-main-backup` 分支。远端
    `git ls-remote` 未返回 `refs/heads/main`，因此本次记录以远端
    HEAD 和实际分支引用为准。

本地 / 远端提交：

  Gemini335 分支的最新两个提交为：

    986ab9f Add Gemini335 setup guide
    0ac754a Add Gemini335 calibration workflow

README 更新：

  `README.md` 已更新为 Gemini335 当前状态说明，主要包括：

    - 当前目标是 UR5 + Orbbec Gemini335 的相机内参、RTDE 位姿读取和
      手眼标定路径验证；
    - 当前 Gemini335 RGB 内参文件位置：
      `calibration/rgb_intrinsics/results/rgb_intrinsics_gemini335_1920x1080.yaml`；
    - 已验证 Gemini335 图像、ChArUco 检测、`ur_rtde` 读取 UR5 pose /
      joints、单帧 `T_base_tool` + `T_camera_target` 采集流程；
    - DaBai 相机时代的手眼结果只作为历史归档，不能直接用于 Gemini335；
    - 常用入口命令改为默认从仓库根目录执行；
    - 增加 `calibration_dabai` 和 `calibration_gemini335` 的分支用途说明。

新增环境配置文档：

  新增：

    docs/SETUP_GEMINI335.md

  内容包括：

    - 从 GitHub 克隆 `calibration_gemini335` 后需要补齐的系统环境；
    - ROS2 Humble、Orbbec camera、UR driver、`ur_rtde`、OpenCV/ChArUco
      等依赖；
    - UR5 机器人 IP `192.168.1.10` 和本机 Host IP `192.168.1.20`；
    - 相机、RTDE、UR driver 的推荐验证顺序；
    - 当前仓库没有包含 `ros2_ws/`，因此不是一键可运行环境；
    - Gemini335 固定件尚未完成，正式多姿态手眼标定结果尚未生成。

本段操作说明：

  本段只记录 GitHub 备份和文档整理结果。没有执行机器人运动，没有使用
  sudo。Git 写操作由用户在终端中完成，Codex 只提供命令和说明。

English:

Goal:

  Back up today's Gemini335-related code, scripts, configuration, logs, and
  documentation to GitHub while keeping it separate from the earlier DaBai
  camera work.

GitHub remote verification:

  Remote repository:

    https://github.com/Zhela8Hz/ur5-RoboticsArm

  Read-only remote checks were run with:

    git ls-remote --symref origin HEAD
    git ls-remote origin

  The remote default HEAD currently points to:

    refs/heads/calibration_gemini335
    commit 986ab9f060d769d6e9016218871e410f80d21b34

  Visible remote branches include:

    calibration_gemini335 -> 986ab9f060d769d6e9016218871e410f80d21b34
    calibration_dabai     -> 1f1a32101caba9c346c4da17b923e5ef1d27e63f
    old-main-backup       -> 1f1a32101caba9c346c4da17b923e5ef1d27e63f
    organize-calibration-layout -> 6bbc92ecb217d917375629431eead073ca942324

  Conclusion:

    The GitHub remote default branch now opens at `calibration_gemini335`,
    which contains today's latest Gemini335 backup commit `986ab9f`. The
    DaBai / old-main content is still preserved in `calibration_dabai` and
    `old-main-backup`. `git ls-remote` did not return `refs/heads/main`, so
    this log records the actual remote HEAD and branch refs observed.

Local / remote commits:

  The latest Gemini335 branch commits are:

    986ab9f Add Gemini335 setup guide
    0ac754a Add Gemini335 calibration workflow

README update:

  `README.md` was updated to describe the current Gemini335 state:

    - UR5 + Orbbec Gemini335 intrinsics, RTDE pose reading, and hand-eye path
      validation are the current focus;
    - the current Gemini335 RGB intrinsics file is
      `calibration/rgb_intrinsics/results/rgb_intrinsics_gemini335_1920x1080.yaml`;
    - Gemini335 image capture, ChArUco detection, `ur_rtde` pose / joint
      reading, and single-frame `T_base_tool` + `T_camera_target` capture were
      verified;
    - DaBai hand-eye results are historical only and must not be reused
      directly for Gemini335;
    - common commands are documented as repository-root commands;
    - branch roles for `calibration_dabai` and `calibration_gemini335` were
      documented.

New setup document:

  Added:

    docs/SETUP_GEMINI335.md

  It documents:

    - required environment after cloning `calibration_gemini335`;
    - ROS2 Humble, Orbbec camera, UR driver, `ur_rtde`, OpenCV/ChArUco
      dependencies;
    - UR5 robot IP `192.168.1.10` and computer Host IP `192.168.1.20`;
    - recommended camera, RTDE, and UR driver verification order;
    - the repository does not include `ros2_ws/`, so it is not a one-command
      runnable environment;
    - the Gemini335 camera mount is not finished yet, so no formal multi-pose
      hand-eye result exists yet.

Operation note:

  This section only records GitHub backup and documentation updates. No robot
  motion was executed. No sudo was used. Git write operations were performed
  by the user in their own terminal; Codex only provided commands and
  explanations.


================================================================================
2026-07-22 dynamic calibration reproduction status
================================================================================

中文：

用户说明：

  今天准备开始复现动态标定，因此从 GitHub 克隆了手眼标定相关程序和结果；
  目前还没有实际跑通动态标定流程。

本机核对结果：

  1. 当前工作区确实存在今天引入的外部仓库目录：

       robotic-arm-lab-vla/

     该目录自身包含 `.git/`，并带有 Gemini335 相关标定文件、结果摘要和
     `PROGRESS_LOG.md`。当前主仓库状态中，该目录表现为未跟踪文件：

       ?? robotic-arm-lab-vla/

  2. 当前项目中确实存在一个实时手眼漂移监控程序：

       calibration/extrinsics/handeye/tools/handeye_realtime_drift_monitor.py

     以及对应启动脚本：

       calibration/extrinsics/handeye/scripts/start_handeye_realtime_monitor.sh

     这两个文件当前也是未跟踪文件，说明尚未纳入当前仓库 Git 版本控制。

  3. `handeye_realtime_drift_monitor.py` 的功能与“实时展现相机画面并标注
     实时动态漂移误差检测”描述相符。程序会订阅：

       /camera/color/image_raw

     使用当前 Gemini335 `RGB` 内参和 `T_tool_camera`，检测大 `ChArUco`
     板，绘制检测到的 marker / corner / 坐标轴，并在 OpenCV 窗口中叠加：

       status
       corners
       drift xyz m
       drift rpy deg
       delta xyz mm
       delta norm mm
       delta rot deg
       history trans mean/max mm
       history rot mean/max deg

  4. 该程序从 TF 查询：

       base -> tool0

     并计算：

       T_base_tool * T_tool_camera * T_drift * T_camera_target

     用第一帧作为 reference pose，后续帧显示相对 reference pose 的位置和
     旋转变化。因此它适合用于固定板场景下观察手眼外参漂移或人为注入
     drift 后的实时误差趋势。

当前未验证事项：

  1. 尚未确认该实时监控程序已经在真实 ROS2 / Gemini335 / UR5 环境中成功启动。
  2. 尚未确认动态标定复现流程已经完整跑通。
  3. 尚未确认 `Ceres` 版本的漂移修正原型已完成依赖安装并可运行。

本段操作说明：

  本段只记录用户说明和只读核对结果。没有连接真实设备，没有执行机器人运动，
  没有使用 `sudo`，没有执行 Git 写操作。

English:

User note:

  The user plans to start reproducing dynamic calibration today, and therefore
  cloned hand-eye calibration programs and results from GitHub. The dynamic
  calibration workflow has not been successfully run yet.

Local verification:

  1. The current workspace does contain an external repository directory added
     today:

       robotic-arm-lab-vla/

     This directory has its own `.git/` and contains Gemini335 calibration
     files, result summaries, and `PROGRESS_LOG.md`. In the main repository
     status, it appears as an untracked path:

       ?? robotic-arm-lab-vla/

  2. The current project does contain a realtime hand-eye drift monitor:

       calibration/extrinsics/handeye/tools/handeye_realtime_drift_monitor.py

     and its launcher:

       calibration/extrinsics/handeye/scripts/start_handeye_realtime_monitor.sh

     Both files are currently untracked in the main repository, so they have
     not been added to Git version control here.

  3. `handeye_realtime_drift_monitor.py` matches the description of showing
     the camera image in realtime and overlaying dynamic drift-error
     diagnostics. It subscribes to:

       /camera/color/image_raw

     uses the current Gemini335 `RGB` intrinsics and `T_tool_camera`, detects
     the large `ChArUco` board, draws detected markers / corners / axes, and
     overlays these values in an OpenCV window:

       status
       corners
       drift xyz m
       drift rpy deg
       delta xyz mm
       delta norm mm
       delta rot deg
       history trans mean/max mm
       history rot mean/max deg

  4. The program queries TF:

       base -> tool0

     and computes:

       T_base_tool * T_tool_camera * T_drift * T_camera_target

     It uses the first valid frame as the reference pose, then displays
     translation and rotation changes relative to that reference pose. This is
     suitable for observing hand-eye extrinsic drift or injected drift trends
     in a fixed-board scene.

Items not yet verified:

  1. It has not been confirmed that the realtime monitor has successfully run
     in the real ROS2 / Gemini335 / UR5 environment.
  2. It has not been confirmed that the full dynamic calibration reproduction
     workflow has run successfully.
  3. It has not been confirmed that the `Ceres` drift-correction prototype has
     all dependencies installed and can run.

Operation note:

  This section only records the user note and read-only verification results.
  No real hardware was connected, no robot motion was executed, no `sudo` was
  used, and no Git write operation was performed.


================================================================================
2026-07-23 hand-eye drift injection and Ceres correction advisor update
================================================================================

中文：

今天主要完善了两套相互隔离的手眼外参漂移相关程序。

分日志：

  docs/HANDEYE_20260723_CERES_DRIFT_INJECTION_LOG.md

完成内容概要：

  1. 实时漂移检测程序继续作为独立工具使用：

       calibration/extrinsics/handeye/tools/handeye_realtime_drift_monitor.py
       calibration/extrinsics/handeye/scripts/start_handeye_realtime_monitor.sh

     该程序用于实时显示相机画面、检测固定 ChArUco 板，并对比原始手眼外参
     和软件注入漂移后的手眼外参所计算出的标定板位姿误差。

  2. 独立 Ceres 修正建议程序继续作为另一套独立窗口程序使用：

       calibration/extrinsics/handeye/tools/handeye_ceres_correction_monitor.py
       calibration/extrinsics/handeye/tools/optimize_handeye_correction_ceres.cpp
       calibration/extrinsics/handeye/scripts/start_handeye_ceres_correction_monitor.sh

     该程序自主判断机械臂静止状态，只采集静止且 ChArUco 检测成功的数据组，
     每新增 5 组数据后分别输出当前 5 组结果和累计结果。

  3. 在 Ceres 修正建议程序中新增了临时软件漂移注入功能。该功能只影响
     Ceres 优化所使用的初始手眼外参，不修改原始 handeye YAML，不更新 ROS TF，
     不自动应用修正量。

  4. 两个程序均支持通过环境变量设置漂移参数：

       HANDEYE_ENABLE_DRIFT_INJECTION
       HANDEYE_DRIFT_X_M / HANDEYE_DRIFT_Y_M / HANDEYE_DRIFT_Z_M
       HANDEYE_DRIFT_RX_DEG / HANDEYE_DRIFT_RY_DEG / HANDEYE_DRIFT_RZ_DEG

  5. 明确了使用方式：

       实时漂移检测程序用于观察 original 与 drifted 的误差趋势；
       Ceres 修正建议程序用于在注入已知错误外参后，观察 Ceres 是否估计出
       近似反方向的建议修正量。

  6. 离线验证结果：

       Python 语法检查通过；
       Bash 启动脚本语法检查通过；
       Ceres 后端编译通过。

当前未验证事项：

  1. 尚未用真实采集的多姿态数据验证 Ceres 是否能恢复 X +5 mm 等已知注入误差。
  2. 图像时间戳与机器人位姿严格同步问题暂时保留，当前默认仍使用 RTDE 作为
     位姿来源，并依靠静止采样降低时间不同步影响。
  3. 所有修正结果仍只是建议值，尚未应用到 ROS TF 或写回标定文件。

操作说明：

  本次记录过程中没有执行机器人运动，没有使用 sudo，没有执行 Git 写操作。

English:

Today focused on two isolated hand-eye drift-related programs.

Detailed log:

  docs/HANDEYE_20260723_CERES_DRIFT_INJECTION_LOG.md

Summary:

  1. The realtime drift monitor remains an independent tool for displaying the
     live camera image, detecting the fixed ChArUco board, and comparing board
     pose consistency between the original and software-drifted hand-eye
     transforms.

  2. The independent Ceres correction advisor remains a separate OpenCV-window
     program. It accepts only static robot-pose groups with successful ChArUco
     detection and runs both current-5-group and cumulative optimizations every
     5 accepted groups.

  3. Temporary software drift injection was added to the Ceres correction
     advisor. It only affects the initial hand-eye transform passed to the Ceres
     backend. It does not modify the original hand-eye YAML, update ROS TF, or
     automatically apply any correction.

  4. Both programs use environment variables for drift parameters, including
     XYZ translation in meters and XYZ rotation in degrees.

  5. The intended workflow is now clear: use the realtime monitor for original
     versus drifted error visualization, and use the Ceres advisor to test
     whether a known injected hand-eye error can be estimated as an opposite
     suggested correction.

Validation:

  Python syntax check passed. Bash launcher syntax check passed. The Ceres
  correction backend compiled successfully.

Items not yet verified:

  The real multi-pose data workflow has not yet been used to confirm that Ceres
  recovers known injected errors such as X +5 mm. Strict image/robot timestamp
  synchronization remains intentionally unresolved for now.

Operation note:

  No robot motion was executed, no sudo was used, and no Git write operation was
  performed while recording this update.


================================================================================
2026-07-24 ChArUco motion image quality diagnostic
================================================================================

中文：

本次新增并试运行了运动过程中 RGB 图像质量与 ChArUco 角点识别质量诊断工具：

  calibration/rgb_intrinsics/tools/charuco_motion_quality.py

工具用途：

  1. 订阅 ROS2 图像 topic，默认 `/camera/color/image_raw`。
  2. 对每帧计算图像清晰度、曝光质量、ChArUco 检测数量和位姿重投影误差。
  3. 默认使用当前 Gemini335 RGB 内参：

       calibration/rgb_intrinsics/results/rgb_intrinsics_gemini335_1920x1080.yaml

  4. 默认使用当前 ChArUco 大板参数：

       6x6 squares
       square_length_m = 0.040
       marker_length_m = 0.030
       dictionary = DICT_6X6_1000
       start_id = 233
       min_charuco_corners = 20

  5. 支持可选 `--use-tf`，用于记录 `base -> tool0` 的线速度和角速度。
     如果未加 `--use-tf`，或 TF 不可用，则输出中的 `speed` 和 `ang` 为 `n/a`。

离线验证：

  1. Python 语法检查通过：

       python3 -m py_compile calibration/rgb_intrinsics/tools/charuco_motion_quality.py

  2. ROS2 环境下 `--help` 导入检查通过。
  3. 短时 dry-run 通过，未接收真实图像时正常退出并生成空 CSV。
  4. 合成 ChArUco 图检测通过：

       markers = 18
       charuco = 25
       laplacian_var = 4625.0

用户实测 CSV：

  calibration/rgb_intrinsics/results/charuco_motion_quality_20260724_201232.csv

用户实测 summary：

  samples: 823
  supports_corners_ratio: 1.000
  pose_ok_ratio: 1.000
  supports_pose_ratio: 0.668
  charuco_corners: median=25.0 p05=22.0 p95=25.0
  quality_score: median=100.0 p05=80.0 p95=100.0
  laplacian_var_roi: median=1347.1 p05=958.2 p95=1674.8
  reprojection_rmse_px: median=0.292 px p05=0.152 px p95=112.672 px
  status_counts: BAD_RMSE=273, GOOD=548, WARN_RMSE=2

进一步只读分析结果：

  GOOD:
    frames = 548 / 823 = 66.6%
    rmse median = 0.219 px
    rmse p95 = 0.492 px
    charuco_corners median = 25

  WARN_RMSE:
    frames = 2 / 823 = 0.2%
    rmse range = 1.046-1.170 px

  BAD_RMSE:
    frames = 273 / 823 = 33.2%
    rmse median = 85.289 px
    rmse p95 = 157.687 px
    charuco_corners median = 25

质量结论：

  1. 当标定板完整入镜、几何关系正常时，运动中的图像质量很好。
     GOOD 帧的重投影误差通常低于 0.5 px，能够支撑 ChArUco 角点识别和位姿估计。

  2. 当前运动过程不是全程稳定可用。约 33.2% 帧为 BAD_RMSE，说明这些帧虽然
     检测到了足够 ChArUco 角点，但位姿重投影误差过大，不能用于可靠位姿。

  3. BAD_RMSE 成段出现，例如样本 170-193、284-329、419-451、628-651、
     653-673、737-823。这更符合运动中标定板局部出画面、边缘视角过大、
     marker 误检或板面不完整导致的几何失真，而不是单纯随机动态模糊。

  4. 如果任务只要求“标定板在画面内时能识别角点”，本次结果很好。
     如果任务要求“机械臂运动全过程都稳定识别角点/位姿”，当前视野覆盖率还不够。
     建议后续在算法侧过滤 `status == GOOD` 或 `reprojection_rmse_px <= 1.0/2.0`。

建议：

  1. 先采集静止基线，再分别采集低速、中速、高速运动数据，对比
     `laplacian_var_roi`、`charuco_corners`、`reprojection_rmse_px` 和 `status`。

  2. 优先调整机械臂运动轨迹、相机朝向或标定板位置，保证运动期间标定板完整入镜。

  3. 若需要把质量和机械臂速度关联起来，运行工具时加：

       --use-tf

     并确认 UR driver 正在发布 `base -> tool0` TF。

操作说明：

  本次记录过程中没有执行机器人运动，没有使用 sudo，没有执行 Git 写操作。


================================================================================
2026-07-24 realtime hand-eye drift monitor field debug, rollback, and RTDE/TF status
================================================================================

中文：

本次继续排查实时手眼漂移检测程序：

  calibration/extrinsics/handeye/tools/handeye_realtime_drift_monitor.py
  calibration/extrinsics/handeye/scripts/start_handeye_realtime_monitor.sh

分日志：

  docs/HANDEYE_20260723_CERES_DRIFT_INJECTION_LOG.md

现场现象：

  1. 机械臂和 ChArUco 大板完全静止时，`original` 基本正常。
  2. 机械臂或标定板运动后，`original trans/rot mean` 会升高。
  3. 当前回退版中，运动停止后该统计不会主动快速回落。

临时修改与回退：

  1. 曾临时加入 image sequence 去重、image stamp 显示、`timestamp_tf`
     pose source 和静止采样门控。
  2. 由于静止门控默认阈值过紧，现场出现 `sample gate` 经常黄色的问题。
  3. 按用户要求，实时监控程序已回退到修改前行为：

       pose source choices: rtde, tf
       default pose source: rtde
       no sample gate
       no stillness threshold filtering
       no timestamp_tf lookup
       no image sequence duplicate suppression

  4. 当前 realtime launcher 使用大板参数：

       square_length_m = 0.040
       marker_length_m = 0.030

  5. 当前 realtime launcher 的旋转漂移环境变量是：

       HANDEYE_DRIFT_ROLL_DEG
       HANDEYE_DRIFT_PITCH_DEG
       HANDEYE_DRIFT_YAW_DEG

     注意：Ceres correction launcher 仍使用 `HANDEYE_DRIFT_RX_DEG / RY / RZ`。

当前解释：

  当前 realtime monitor 的 `original trans/rot mean` 是历史样本相对 locked
  reference 的统计，不是当前单帧误差。程序没有运动过滤，运动过程中的样本会被
  append 到历史窗口。如果标定板移动，原 reference 必须按 `r` 重建。如果只移动
  机械臂且板不动，但历史窗口刷新后仍不恢复，应继续查图像时间与机器人位姿时间
  不同步、hand-eye 跨姿态一致性、ChArUco 位姿估计和 active TCP offset。

RTDE / TF / UR driver 检查：

  1. `rtde_receive.RTDEReceiveInterface("192.168.1.10")` 可读：

       getActualTCPPose: readable
       getActualQ: readable

  2. 机器人网络端口可达：

       30001 primary
       30004 RTDE
       29999 dashboard

  3. 检查时 ROS graph 只有相机节点，`/joint_states` 不存在，`base -> tool0`
     TF 不存在。

  4. 尝试启动 `scripts/robot/start_ur5_driver.sh`，未发送任何运动命令。启动失败：

       [FATAL] [URPositionHardwareInterface]: Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)
       ERROR: UR5 driver failed after 3 attempts due to configuration package timeout.

  5. Dashboard 只读状态：

       Robotmode: RUNNING
       Safetystatus: NORMAL
       programState: STOPPED null
       get loaded program: No program loaded

结论：

  1. RTDE 当前可继续作为只读位姿来源。
  2. UR driver 当前不可用，`HANDEYE_POSE_SOURCE=tf` 暂时不能用于 realtime monitor。
  3. 若后续要启用 TF，需要先在示教器加载并播放只包含 External Control 的程序，
     或由用户单独明确授权 `headless_mode:=true` 启动。

操作说明：

  本次没有执行机器人运动，没有使用 sudo，没有执行 Git 写操作。日志为追加记录。

English:

This update records the field debugging and rollback of the realtime hand-eye
drift monitor.

Detailed log:

  docs/HANDEYE_20260723_CERES_DRIFT_INJECTION_LOG.md

The monitor was temporarily extended with image sequence de-duplication, image
stamp display, `timestamp_tf`, and a stillness sample gate. Because the default
stillness thresholds were too tight in the live RTDE signal, the gate frequently
turned yellow even while the robot appeared static. At the user's request, the
realtime monitor was rolled back to the previous basic behavior: `rtde` or
latest `tf` pose source, no sample gate, no `timestamp_tf`, and no image
sequence duplicate suppression.

The current large-board realtime launcher uses:

  square_length_m = 0.040
  marker_length_m = 0.030

The current realtime rotation drift variables are `HANDEYE_DRIFT_ROLL_DEG`,
`HANDEYE_DRIFT_PITCH_DEG`, and `HANDEYE_DRIFT_YAW_DEG`. The Ceres correction
launcher still uses `HANDEYE_DRIFT_RX_DEG / RY / RZ`.

The observed behavior is consistent with the current statistic design:
`original trans/rot mean` is a history-window error against a locked reference,
not a current-frame-only value. Motion frames are appended. If the board moves,
the reference must be rebuilt with `r`. If only the robot moves and the board
stays fixed, persistent high error after the history window refreshes points to
image/robot time mismatch, cross-pose hand-eye inconsistency, ChArUco pose
issues, or an active TCP offset mismatch.

RTDE was verified readable, and robot ports 30001 / 30004 / 29999 were
reachable. The UR driver was not available in ROS at check time: `/joint_states`
and `base -> tool0` TF were missing. A no-motion startup attempt of
`scripts/robot/start_ur5_driver.sh` failed with:

  [FATAL] [URPositionHardwareInterface]: Could not get configuration package within timeout, are you connected to the robot?(Configured timeout: 1 sec)
  ERROR: UR5 driver failed after 3 attempts due to configuration package timeout.

Dashboard status was `Robotmode: RUNNING`, `Safetystatus: NORMAL`,
`programState: STOPPED null`, and no loaded program. Therefore
`HANDEYE_POSE_SOURCE=tf` is not usable until the UR driver is successfully
started.

Operation note:

  No robot motion command was executed, no sudo was used, and no Git write
  operation was performed. This was an append-only log update.

Continuous drift correction monitor (not yet field-tested):

  `calibration/extrinsics/handeye/tools/handeye_continuous_drift_correction_monitor.py`
  reads RGB images and timestamp-matched `base -> tool0` TF, detects possible
  hand-eye drift, automatically collects qualified observations, and uses Ceres
  to calculate a candidate correction. The display shows the correction and
  validation error before and after applying it to the current extrinsic.

  The candidate is virtual only: the program does not write a new hand-eye YAML,
  publish TF, or command robot motion. This workflow has not yet been tested on
  the real Gemini 335 camera and UR5 environment.

2026-08-01 Dynamic SE(3) correction baseline note
-------------------------------------------------

Detailed log:

  docs/DYNAMIC_HANDEYE_SE3_CORRECTION_LOG.md

Summary:

  Today's dynamic injection tests showed that the injected run's total correction
  must be compared against an independently collected no-injection dynamic
  baseline. The injected total correction includes both the real dynamic system
  residual and the inverse of the virtual injection, so judging the injected run
  alone is misleading.

  No-injection baseline candidate:

    dynamic_se3_20260801_142459/results/correction_020.yaml
    DeltaX_tool = [+2.501, +4.093, -0.761] mm

  Tool X +5 mm injection candidate:

    dynamic_se3_20260801_134944/results/correction_020.yaml
    DeltaX_tool = [-2.564, +4.017, -0.532] mm
    Relative to baseline = [-5.066, -0.076, +0.228] mm

  Tool Y -5 mm injection candidate:

    dynamic_se3_20260801_144040/results/correction_020.yaml
    DeltaX_tool = [+2.565, +8.504, -2.922] mm
    Relative to baseline = [+0.064, +4.411, -2.161] mm

  Therefore the X injection result is close to the expected inverse injection,
  while the Y injection has the correct sign and a clear Y response but still
  contains Z coupling. These runs were candidate results only; the latest
  displayed sessions stopped at stable correction updates 1/2.

  The live dynamic correction script now supports `--no-injection` and prints
  `Candidate correction (not yet validated)` before the strict validation gate
  is fully passed.

Operation note:

  No robot motion command was executed by Codex, no sudo was used, and no Git
  write operation was performed.
